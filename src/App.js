import React from 'react';
import Button from './components';

const App = () => (
    <div>
        <Button size={25}>
            Primary Button
        </Button>
        <Button color="secondary">
            Secondary Button
        </Button>
    </div>
);

export default App;
